name: Topology Validation

on:
  push:
    branches: [main]

jobs:
  topology-tests:
    runs-on: ubuntu-latest  
    steps:
        - name: Checkout repo
          uses: actions/checkout@v4

        - name: Setup Python
          uses: actions/setup-python@v5
          with:
            python-version: "3.12"

        - name: Install dependencies
          run: |
            python -m pip install --upgrade pip
            pip install -r requirements.txt
            pip install pytest

        - name: Run topology validation
          run: | 
           python -m pytest tests/test_topology.py -vv --tb=long